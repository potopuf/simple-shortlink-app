<!DOCTYPE html>
<html>
  <head>
    <title>SimpleShortlinkApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
        <img width="40" height="40" src="/kitty.png" alt="Kitty label"/>
        <span class="fs-4">Simple Shortlink app</span>
      </a>

        <% if action_name == 'index'%>
        <ul class="nav nav-pills">
          <li class="nav-item"><%= link_to 'New',
                                           new_shortlink_path,
                                           class: 'nav-link active',
                                           aria: { current: 'page' } %></li>
        </ul>
        <% end %>
    </header>
    <%= yield %>
    <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
      <div class="col-md-4 d-flex align-items-center">
        <a href="/" class="mb-3 me-2 mb-md-0 text-muted text-decoration-none lh-1">
          <img width="40" height="40" src="/kitty.png" alt="Kitty label"/>
        </a>
        <span class="mb-3 mb-md-0 text-muted">&copy; 2022 United Shortlinks, Inc</span>
      </div>
    </footer>
  </div>
  </body>
</html>
